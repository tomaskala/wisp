#ifndef WISP_COMMON_H
#define WISP_COMMON_H

#include <inttypes.h>
#include <stdbool.h>
#include <stddef.h>
#include <stdint.h>

#define UINT8_COUNT (UINT8_MAX + 1)

#define XSTR(s) STR(s)
#define STR(s) #s

struct wisp_state;

#endif
